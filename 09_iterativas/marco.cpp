#include<stdio.h>
#include<stdlib.h>

int main (){

    for (int c=0 ; c<5 ; c++ )
          printf("*");
          printf("\n");
    for (int b=0 ; b<3 ; b++ )
          printf("*   *\n");
    for (int a=0 ; a<5 ; a++ )
          printf("*");
          printf("\n");


  for(int f=0 ; f<N ; f++ )
      for(int c=0, c<N ; f++)
          if ( c== 0 ||c == N-1 || f==0 || f==N-1 || f==c || f+c==N-1)
          printf("*");
              else
                  printf(" ");

      printf("\n");


  return 0;
}
